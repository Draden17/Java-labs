/**
 * SortThreeNumbers.java
 * COP2250 - Assignment 6 (Exercise 6.5)
 *
 * Name: Your Name
 * Date: Today's Date
 * Description: This program reads three numbers from the user,
 * sorts them in increasing order, and displays them.
 */

import java.util.Scanner;

public class SortThreeNumbers {

    // STEP 1: Method signature
    public static void displaySortedNumbers(double num1, double num2, double num3) {

        // STEP 2: Sort the three numbers
        double temp;

        if (num1 > num2) {
            temp = num1;
            num1 = num2;
            num2 = temp;
        }

        if (num1 > num3) {
            temp = num1;
            num1 = num3;
            num3 = temp;
        }

        if (num2 > num3) {
            temp = num2;
            num2 = num3;
            num3 = temp;
        }

        // STEP 3: Display the sorted numbers
        System.out.println(num1 + " " + num2 + " " + num3);
    }

    public static void main(String[] args) {

        // STEP 6: Create a Scanner
        Scanner input = new Scanner(System.in);

        // STEP 7: Prompt the user
        System.out.print("Enter three numbers: ");

        // STEP 8: Read three numbers
        double num1 = input.nextDouble();
        double num2 = input.nextDouble();
        double num3 = input.nextDouble();

        // STEP 9: Call method
        displaySortedNumbers(num1, num2, num3);

        // STEP 10: Close Scanner
        input.close();
    }
}
